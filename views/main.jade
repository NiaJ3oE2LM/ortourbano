.panel
  .panel-heading
    h3.panel-title Real time parameters
  .panel-body
    .panel-heading
      h4.panel-title temperature
    .progress
      .progress-bar.progress-bar-danger(role='progressbar.progress-bar-success', aria-valuenow='{{temp}}', aria-valuemin='0', aria-valuemax='100', style='width: {{temp}}%;')
        {{temp}}°C
    .panel-heading
      h4.panel-title light
    .progress
      .progress-bar.progress-bar-warning(role='progressbar', aria-valuenow='{{light}}', aria-valuemin='0', aria-valuemax='100', style='width: {{light*10}}%;')
        {{light}} ?
    .panel-heading
      h4.panel-title humidity
    .progress
      .progress-bar.progress-bar-info(role='progressbar', aria-valuenow='{{hum}}', aria-valuemin='0', aria-valuemax='100', style='width: {{hum/15}}%;')
        {{hum}} ?
      
.panel
  .panel-heading
    h3.panel-title Recent values
  .panel-body
    #graph
    
.panel
  .panel-heading
    h3.panel-title Tools
  .panel-body
    .row
      .col-xs-4
        a.thumbnail(data-toggle="modal", data-target="#m1")
          img(src='public/ic_all_out_48px.svg')
          .caption
            h3 Fix it
            p some details
      .col-xs-4
        a.thumbnail(href='')
          img(src='public/ic_all_out_48px.svg')
          .caption
            h3 Fix it
            p some details
      .col-xs-4
        a.thumbnail(href='')
          img(src='public/ic_all_out_48px.svg')
          .caption
            h3 Fix it
            p some details
div(style="height:5em;")

#m1.modal.fade(tabindex='-1', role='dialog', aria-labelledby='myModalLabel')
  .modal-dialog(role='document')
    .modal-content
      .modal-header
        button.close(type='button', data-dismiss='modal', aria-label='Close')
          span(aria-hidden='true') ×
        h4#myModalLabel.modal-title Modal title
      .modal-body
        | ...
      .modal-footer
        button.btn(type='button') Fire!
